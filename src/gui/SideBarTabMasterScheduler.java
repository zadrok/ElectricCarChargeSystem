package gui;

import javax.swing.*;

public class SideBarTabMasterScheduler extends SideBarTab
{
	
	public SideBarTabMasterScheduler(SideBar aSideBar)
	{
		super(aSideBar);
		
		int xInfoOffset = getSideBar().getWidth()/2;
		int yOffset = 0;
		int yOffsetIncrement = 20;
		int width = getSideBar().getWidth();
		int rightMargin = 20;
		int height = 20;

		yOffset += yOffsetIncrement;
		// add something here
		
		
	}
	
}
